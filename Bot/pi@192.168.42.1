from discovery_bot import pins
from discovery_bot import Movement
from discovery_bot import Ultrasound

class Move:
	def __init__(self, start = (0,0)):
		self.location = start
		self.x = start[0]
		self.y = start[1]

	def move(self, x ,y):
		self.y += y
		self.x += x

		self.location = (self.x,self.y)

	def turn(self,angle):

		if (angle == 'left' or angle == -90 or angle == 270):
			movement.turn_left()
		else if (angle == 'right' or angle == 90 or angle == -270):
			movement.turn_right()
		else if (angle == 'around' or angle ==180):
			movement.turn_right()
			movement.turn_right()
		else if (angle == 360):
			for i in range (1..4):
				movement.turn_right()
bot = Move()
bot.turn(90)
